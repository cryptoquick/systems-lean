#!/usr/bin/env bash
# SPDX-License-Identifier: Unlicense
# Pre-commit: pure flake hygiene + residual workspace gates (via just).
set -euo pipefail
root=$(cd "$(dirname "$0")/../.." && pwd)
cd "$root"
exec just pre-commit
